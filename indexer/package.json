{
  "name": "hivemind-indexer",
  "version": "1.0.0",
  "description": "Hivemind event indexer + WebSocket server — live state sync for the frontend",
  "type": "module",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.27.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
